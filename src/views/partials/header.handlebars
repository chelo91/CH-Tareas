<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <span class="navbar-brand mb-0 h1">Tarea 5</span>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {{#each pages}}
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{this.url}}">{{this.title}}</a>
                </li>
                {{/each}}
                {{#if user.isUser}}
                <li class="nav-item">
                    <a id="cart-link" class="nav-link" aria-current="page" href="/carts/{{user.cart}}">Cart</a>
                </li>
                {{/if}}

            </ul>
        </div>
        <div class="col-md-3 text-end">
            {{#if user}}
            <a href="/profile"><span class="me-2">Hola {{user.first_name}}</span></a>
            <a type="button" class="btn btn-outline-primary me-2" href="/logout">Logout</a>
            {{else}}
            <a type="button" class="btn btn-outline-primary me-2" href="/login">Iniciar</a>
            <a type="button" class="btn btn-primary" href="/register">Registrar</a>
            {{/if}}

        </div>
    </div>
</nav>
<script>
    /*document.addEventListener("DOMContentLoaded", function () {
        const cartLink = document.getElementById('cart-link');
        const cartValue = localStorage.getItem("cart");
        cartLink.href = "/carts/" + cartValue;
    });*/

</script>